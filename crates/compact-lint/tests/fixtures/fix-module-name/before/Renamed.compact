/**
 * @module Stale<T> (archived)
 * @description The tag still names the module's old name.
 */
export module Renamed {
  /**
   * @module Inner.
   * @description The inner tag names nothing that exists.
   */
  export module Nested {
  }
}
