pragma language_version >= 0.23.0;

/**
 * @module Sections
 * @description A module whose header stops before its security section.
 *
 * @notice Privacy:
 * - Amounts stay private.
 *
 * @dev Notation:
 * - `H(...)`: a `Bytes<32>` digest.
 */
export module Sections {
}
