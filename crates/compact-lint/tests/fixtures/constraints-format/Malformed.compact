/**
 * @module Malformed
 * @description A module whose annotations do not parse.
 */
export module Malformed {
  /**
   * @description The comma is missing.
   * @constraints k=10 rows=626
   */
  export circuit first(): [] { }

  /**
   * @description The fields are the wrong way round.
   * @constraints rows=626, k=10
   */
  export circuit second(): [] { }
}
